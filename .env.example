# This static site requires no application environment variables or secrets.
# Optional CLI-only TLS trust setting used on this Windows workstation:
# NODE_OPTIONS=--use-system-ca
# Set it only for the current PowerShell process when Node needs the Windows
# certificate store; this keeps certificate verification enabled.
# Do not configure it as a Worker binding or commit Cloudflare credentials.
